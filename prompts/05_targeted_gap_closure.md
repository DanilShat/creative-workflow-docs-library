We found concrete gaps after review or first-run testing.

Close them completely.

Rules:
- fix the actual gaps, not just documentation wording;
- keep architecture intact;
- keep tests meaningful;
- update runbook and comments if implementation changed;
- do not leave “we can do this later” for issues that block a working MVP.

At the end provide:
1. the exact gaps you fixed,
2. files changed,
3. how the operator should re-test the fixed paths,
4. whether any manual cleanup or migration step is needed.
